spring:
  application:
    name: Chat-App

fakestore:
  base-url: https://fakestoreapi.com

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
